# OS / Editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Python bytecode caches (locally run scripts)
__pycache__/
*.pyc
*.pyo

# Build artefacts
*.tar
*.tar.gz
*.log
# Local Claude Code / agent config (never commit)
.claude/

# Importable Unraid template deliverable (copy of templates/jdownloader.xml — regenerated on each template change)
my-JDownloader.xml

# proprietary font used at build time to render the wordmark to paths (never committed)
.github/assets/_fonts/

# Local-only review/unit tests + JDK scratch (never push)
local-tests/
local-tests-out/
